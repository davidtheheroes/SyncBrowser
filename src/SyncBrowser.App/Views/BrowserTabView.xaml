<UserControl x:Class="SyncBrowser.App.Views.BrowserTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:converters="clr-namespace:SyncBrowser.App.Converters">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Border CornerRadius="8" 
            BorderThickness="2"
            Margin="4"
            ClipToBounds="True">
        <Border.BorderBrush>
            <SolidColorBrush Color="{Binding BorderColor}"/>
        </Border.BorderBrush>
        <Border.Background>
            <SolidColorBrush Color="#161B22"/>
        </Border.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ═══ Header Bar ═══ -->
            <Border Grid.Row="0" Padding="8,6" Background="#21262D" CornerRadius="6,6,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Role Badge -->
                    <Border Grid.Column="0" CornerRadius="4" Padding="8,3" Margin="0,0,6,0"
                            VerticalAlignment="Center">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#30363D"/>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMaster}" Value="True">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                    <GradientStop Color="#FFD700" Offset="0"/>
                                                    <GradientStop Color="#FFA500" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{Binding RoleBadge}" FontSize="10" FontWeight="Bold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#8B949E"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMaster}" Value="True">
                                            <Setter Property="Foreground" Value="#0D1117"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Border>

                    <!-- Navigation Buttons -->
                    <Button Grid.Column="1" Content="◀" Command="{Binding GoBackCommand}"
                            Style="{StaticResource DarkButton}" Padding="6,4" FontSize="11" Margin="0,0,2,0"/>
                    <Button Grid.Column="2" Content="▶" Command="{Binding GoForwardCommand}"
                            Style="{StaticResource DarkButton}" Padding="6,4" FontSize="11" Margin="0,0,4,0"/>

                    <!-- URL Bar -->
                    <TextBox Grid.Column="3" 
                             Text="{Binding CurrentUrl, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource DarkTextBox}"
                             FontSize="12"
                             VerticalAlignment="Center"
                             Margin="0,0,4,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Return" Command="{Binding NavigateCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <!-- Action Buttons -->
                    <Button Grid.Column="4" Content="🔄" Command="{Binding ReloadCommand}"
                            Style="{StaticResource DarkButton}" Padding="6,4" FontSize="11" Margin="0,0,2,0"/>
                    <Button Grid.Column="5" Content="👑" Command="{Binding SetAsMasterCommand}"
                            Style="{StaticResource DarkButton}" Padding="6,4" FontSize="11" Margin="0,0,2,0"
                            ToolTip="Set as Master"/>
                    <Button Grid.Column="6" Content="✕" 
                            Style="{StaticResource DarkButton}" Padding="6,4" FontSize="11"
                            Click="OnCloseTabClick"
                            ToolTip="Close Tab"/>
                </Grid>
            </Border>

            <!-- ═══ WebView2 Browser ═══ -->
            <wv2:WebView2 Grid.Row="1" 
                          x:Name="WebView"
                          DefaultBackgroundColor="#0D1117"/>

            <!-- Loading Overlay -->
            <Border Grid.Row="1" 
                    Background="#80000000" 
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="Loading..." 
                           Foreground="White" 
                           FontSize="16"
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
